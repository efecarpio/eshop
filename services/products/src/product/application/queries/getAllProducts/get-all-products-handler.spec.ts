import { GetAllProductsHandler } from './get-all-products-handler';

describe('GetAllProductsHandler', () => {
  it('should be defined', () => {
    expect(new GetAllProductsHandler()).toBeDefined();
  });
});

import { SearchProductRequest } from './search-product-request';

describe('SearchProductRequest', () => {
  it('should be defined', () => {
    expect(new SearchProductRequest()).toBeDefined();
  });
});
